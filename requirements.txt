# Se usan límites mínimos (>=) en vez de versiones exactas: para Python 3.13
# conviene dejar que pip resuelva la versión más reciente compatible de cada
# paquete, en vez de fijar una versión exacta que pudo haber sido publicada
# antes de que existiera soporte para 3.13.
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.2
tenacity>=9.0.0
transformers>=4.46.0
accelerate>=1.0.1
bitsandbytes>=0.45.3
sentencepiece>=0.2.0
python-dotenv>=1.0.1
# torch se instala en el Dockerfile con la variante CUDA correcta
